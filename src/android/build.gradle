repositories{
    jcenter()
    flatDir{
        dirs 'aar'
    }
}
dependencies {
    compile(name:'matisse', ext:'aar')
}
android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}